<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.xingyu.system.ccq.mapper.UserProfilesMapper">
    
    <resultMap type="UserProfiles" id="UserProfilesResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="realName"    column="real_name"    />
        <result property="avatarUrl"    column="avatar_url"    />
        <result property="createdAt"    column="created_at"    />
        <result property="updatedAt"    column="updated_at"    />
    </resultMap>

    <sql id="selectUserProfilesVo">
        select id, user_id, real_name, avatar_url, created_at, updated_at from user_profiles
    </sql>

    <select id="selectUserProfilesList" parameterType="UserProfiles" resultMap="UserProfilesResult">
        <include refid="selectUserProfilesVo"/>
        <where>  
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="realName != null  and realName != ''"> and real_name like concat('%', #{realName}, '%')</if>
            <if test="avatarUrl != null  and avatarUrl != ''"> and avatar_url = #{avatarUrl}</if>
            <if test="createdAt != null "> and created_at = #{createdAt}</if>
            <if test="updatedAt != null "> and updated_at = #{updatedAt}</if>
        </where>
    </select>
    
    <select id="selectUserProfilesById" parameterType="Long" resultMap="UserProfilesResult">
        <include refid="selectUserProfilesVo"/>
        where id = #{id}
    </select>

    <insert id="insertUserProfiles" parameterType="UserProfiles" useGeneratedKeys="true" keyProperty="id">
        insert into user_profiles
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="realName != null">real_name,</if>
            <if test="avatarUrl != null">avatar_url,</if>
            <if test="createdAt != null">created_at,</if>
            <if test="updatedAt != null">updated_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="realName != null">#{realName},</if>
            <if test="avatarUrl != null">#{avatarUrl},</if>
            <if test="createdAt != null">#{createdAt},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
         </trim>
    </insert>

    <update id="updateUserProfiles" parameterType="UserProfiles">
        update user_profiles
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteUserProfilesById" parameterType="Long">
        delete from user_profiles where id = #{id}
    </delete>

    <delete id="deleteUserProfilesByIds" parameterType="String">
        delete from user_profiles where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>